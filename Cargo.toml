[workspace]
resolver = "3"
members = ["crates/*"]

# crates/model    — chart option SSoT, data containers, preset policies (dependency-free).
# crates/renderer — wgpu + skia hybrid renderer; depends on model and re-exports it.
